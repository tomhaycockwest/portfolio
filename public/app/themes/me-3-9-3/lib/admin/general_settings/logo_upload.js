jQuery(document).ready(function() {

     /*
     var uploadID = ''; setup the var*

jQuery('.upload-button').click(function() {
    uploadID = jQuery(this).prev('input');
    formfield = jQuery('.upload').attr('name');
    tb_show('', 'media-upload.php?type=image&amp;TB_iframe=true');
    return false;
});

window.send_to_editor = function(html) {
    imgurl = jQuery('img',html).attr('src');
    uploadID.val(imgurl);
    tb_remove();
};
     */
     /*
     //Logo url
     jQuery('#logo_url_button').click(function() {
               formfield = jQuery('#logo_url').attr('name');
               tb_show('', 'media-upload.php?type=image&amp;TB_iframe=true');
               return false;
     });
     
     //Logo url
     window.send_to_editor = function(html) {
               imgurl = jQuery('img',html).attr('src');
               jQuery('#logo_url').val(imgurl);
               tb_remove();
     }
     
     //Shortcut url
     jQuery('#shortcut_icon_button').click(function() {
               formfield = jQuery('#shortcut_icon').attr('name');
               tb_show('', 'media-upload.php?type=image&amp;TB_iframe=true');
               return false;
     });

     window.send_to_editor = function(html) {
               shortcut_url = jQuery('img',html).attr('src');
               jQuery('#shortcut_icon').val(shortcut_url);
               tb_remove();
     }
     
     //Touchicon1 url
     jQuery('#touch_icon1_button').click(function() {
               formfield = jQuery('#touch_icon1').attr('name');
               tb_show('', 'media-upload.php?type=image&amp;TB_iframe=true');
               return false;
     });

     window.send_to_editor = function(html) {
               shortcut_url = jQuery('img',html).attr('src');
               jQuery('#touch_icon1').val(shortcut_url);
               tb_remove();
     }
     
     //Touchicon2 url
     jQuery('#touch_icon2_button').click(function() {
               formfield = jQuery('#touch_icon2').attr('name');
               tb_show('', 'media-upload.php?type=image&amp;TB_iframe=true');
               return false;
     });

     window.send_to_editor = function(html) {
               shortcut_url = jQuery('img',html).attr('src');
               jQuery('#touch_icon2').val(shortcut_url);
               tb_remove();
     }
     
     //Touchicon3 url
     jQuery('#touch_icon3_button').click(function() {
               formfield = jQuery('#touch_icon3').attr('name');
               tb_show('', 'media-upload.php?type=image&amp;TB_iframe=true');
               return false;
     });

     window.send_to_editor = function(html) {
               shortcut_url = jQuery('img',html).attr('src');
               jQuery('#touch_icon3').val(shortcut_url);
               tb_remove();
     }
     
     //Touchicon4 url
     jQuery('#touch_icon4_button').click(function() {
               formfield = jQuery('#touch_icon4').attr('name');
               tb_show('', 'media-upload.php?type=image&amp;TB_iframe=true');
               return false;
     });

     window.send_to_editor = function(html) {
               shortcut_url = jQuery('img',html).attr('src');
               jQuery('#touch_icon4').val(shortcut_url);
               tb_remove();
     }
     */
});